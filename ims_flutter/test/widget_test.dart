import 'package:flutter_test/flutter_test.dart';
import 'package:ims_flutter/main.dart';

void main() {
  testWidgets('App class exists', (WidgetTester tester) async {
    expect(ImsApp, isNotNull);
  });
}
